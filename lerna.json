{
  "$schema": "https://json.schemastore.org/lerna.json",
  "npmClient": "pnpm",
  "version": "independent",
  "packages": ["packages/*"],
  "command": {
    "version": {
      "conventionalCommits": true
    },
    "publish": {
      "ignoreChanges": ["*.md"]
    }
  }
}
